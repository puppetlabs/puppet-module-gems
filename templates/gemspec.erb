Gem::Specification.new do |s|
  s.name        = '<%= gem_name %>'
  s.version     = '<%= gem_version %>'
  s.date        = '<%= build_date %>'
  s.summary     = '<%= info[summary] %>'
  s.authors     = '<%= info[authors] %>'
  s.email       = '<%= info[email] %>'
  s.homepage    = '<%= info[homepage] %>'
  s.license     = '<%= info[licenses] %>'

  # Gem Dependencies
  <%- Array(dependencies).each do |dep| -%>
    <%- if dep['version'] != nil -%>
      <%- dep_versions = ", #{Array(dep['version']).map { |s| "'#{s}'"}.join(', ')}" -%>
    <%- end -%>
  s.add_runtime_dependency '<%= dep['gem'] %>'<%= dep_versions unless dep_versions.nil? %>
  <%- end -%>
end
